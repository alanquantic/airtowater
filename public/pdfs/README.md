# PDFs

Drop your `.pdf` files in this folder. They are served as static assets (URL: `/pdfs/<filename>.pdf`).

To make a PDF appear in the gallery at `/pdfs`, edit the manifest in `src/data/pdfs.js` and add an entry pointing to `/pdfs/<filename>.pdf`.
